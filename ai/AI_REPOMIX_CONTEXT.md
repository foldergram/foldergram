# AI Repomix Context

This file is a stub. Do not treat it as project context.

- Default work uses `ai/AI_CONTEXT.md`, `scripts/ai-symbol.sh`, and `scripts/ai-search.sh`.
- Full dump is generated only with `AI_REFRESH_REPOMIX=1 scripts/ai-refresh.sh`.
- The dump path is `.tmp/AI_REPOMIX_CONTEXT.md`, and even that should be sampled, not fully loaded.
